# 윈도우 서버용 API 테스트 패키지 요구사항
# 맥에서 작업한 파일을 윈도우 서버에서 테스트할 때 필요한 패키지들

# 기본 HTTP 요청 라이브러리
requests>=2.31.0
urllib3>=2.0.0

# JSON 처리
jsonschema>=4.19.0

# 로깅 및 모니터링
colorlog>=6.7.0
psutil>=5.9.0

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0

# 웹 서버 및 API
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# WebSocket 지원
websockets>=11.0.0
websocket-client>=1.6.0

# 데이터베이스 (선택사항)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# 환경 변수 관리
python-dotenv>=1.0.0

# 날짜/시간 처리
python-dateutil>=2.8.0

# 암호화 (API 키 보안)
cryptography>=41.0.0

# 멀티프로세싱
multiprocessing-logging>=0.3.0

# 테스트 관련
pytest>=7.4.0
pytest-asyncio>=0.21.0

# 성능 모니터링
memory-profiler>=0.61.0

# 윈도우 특화 패키지
pywin32>=306; sys_platform == "win32"
wmi>=1.5.1; sys_platform == "win32"

# 개발 도구
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0 