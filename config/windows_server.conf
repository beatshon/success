# Windows 서버 연결 설정
# 실제 Windows 서버 정보로 수정하세요

# Windows 서버 IP 주소
WINDOWS_HOST="192.168.1.100"

# Windows 사용자명
WINDOWS_USER="user"

# Windows 서버의 프로젝트 경로
WINDOWS_PATH="/path/to/kiwoom_trading"

# SSH 포트 (기본값: 22)
SSH_PORT="22"

# API 서버 포트 (기본값: 8080)
API_PORT="8080" 